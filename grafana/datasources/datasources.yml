apiVersion: 1

datasources:
  - name: datasource-prometheus
    uid: P02FBFF047EDBB13A
    type: prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      timeInterval: 15s
      manageAlerts: true
      prometheusType: Prometheus
